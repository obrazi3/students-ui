/**
 * this test is needed only for successfully passing npm test command when there are no tests in the app
 * otherwise the command will fail
 */
describe('this test should be removed after adding one more+', () => {
	it('should pass', () => {
		expect(true).toBeTruthy();
	});
});
